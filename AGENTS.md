# AGENTS.md

이 저장소는 Unity 프로젝트입니다. Codex는 아래 규칙을 우선해서 작업합니다.

## 기본 탐색 범위

- 기본적으로 `Assets/Scripts` 폴더만 읽습니다.
- `Library`, `Temp`, `Obj`, `Logs`, `UserSettings` 폴더는 절대 읽지 않습니다.
- `Packages`와 `ProjectSettings`는 오류 해결에 필요한 경우에만 읽습니다.

## 수정 제한

- 사용자가 명시적으로 요청하지 않는 한 Scene, Prefab, Material 파일은 수정하지 않습니다.
- 기존 기능과 동작을 최대한 유지합니다.
- 변경은 요청된 작업을 해결하는 데 필요한 최소 범위로 제한합니다.

## 작업 절차

- 수정 전, 어떤 파일을 수정할지 먼저 사용자에게 알립니다.
- 수정 후, 변경 사항을 간단히 요약합니다.
